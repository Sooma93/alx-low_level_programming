#include <stdio.h>
/**
 * main - Entery point
 * Descrption: alphber reversed
 * Return: Always 0
 */
int main(void)
{
	int m = 122;

	while (m >= 97)
	{
		putchar(m);
		m--;
	}
	putchar('\n');
		return (0);
}
